# Local development only — these values are NOT baked into production builds.
# All keys are entered by users in the Settings tab of the app.
# VITE_GITHUB_OWNER and VITE_GITHUB_REPO are the only values used in production.
VITE_GITHUB_OWNER=juliengustinelli-ops
VITE_GITHUB_REPO=LexAi
